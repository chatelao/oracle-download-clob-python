name: CI

on:
  push:
    branches: [ "**" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
        cache-dependency-path: |
          src/requirements.txt
          test/requirements-test.txt

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        bash src/install.sh
        bash test/install.sh

    - name: Verify project structure
      run: |
        python test/verify_project.py

    - name: Run tests
      run: |
        pytest

  package:
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: 'pip'
        cache-dependency-path: |
          src/requirements.txt
          src/requirements-packaging.txt

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r src/requirements.txt
        pip install -r src/requirements-packaging.txt

    - name: Build with PyInstaller
      run: |
        export PYTHONPATH=$PYTHONPATH:$(pwd)
        pyinstaller --onefile --name oracle-clob-tool src/cli.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: oracle-clob-tool
        path: dist/oracle-clob-tool

    - name: Release
      uses: softprops/action-gh-release@v2
      with:
        files: dist/oracle-clob-tool
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
